# GameCut AI — copy to .env and fill in optional keys
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# If FFmpeg isn't on PATH, set full paths:
# FFMPEG_PATH=C:\ffmpeg\bin\ffmpeg.exe
# FFPROBE_PATH=C:\ffmpeg\bin\ffprobe.exe
